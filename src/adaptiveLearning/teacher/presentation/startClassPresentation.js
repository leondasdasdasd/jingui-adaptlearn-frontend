export function formatStartClassStep(step) {
  return step?.title || "";
}

export function startClassIssueText(issue) {
  return issue || "未知问题";
}

export const startClassText = {
  title: "上课配置",
  startBtn: "开始上课",
};
