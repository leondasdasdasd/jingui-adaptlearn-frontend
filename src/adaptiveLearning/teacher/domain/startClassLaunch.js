export function validateStartClassConfig(config) {
  return { valid: true, errors: [] };
}

export function buildStartClassLaunch(config) {
  return config;
}

export function buildStartClassPayload(config) {
  return config;
}
